<!doctype html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="x-ua-compatible" content="ie=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
        <title>Thank you !</title>
    <style>

        body {
            background: #FAFAFA;
        }
        .thank-container {
            width: 540px;
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            font-family: 'Roboto', sans-serif;
            box-shadow: 1px 1px 13px 0px rgba(128, 128, 128, 0.35);
            background: white;
            padding: 10px 50px 30px;
            margin-top: 50px;
        }

        .done {
            display: flex;
            flex-direction: row;
            align-items: center;
            padding: 20px;
            font-size:24px;
        }

        .done i {
            color: #828282;
            font-size: 60px;
            margin-right: 10px;
        }

        .done span {
            color: #4FC3F7;
            text-align: center;

        }

        button {
            background: #8BC34A;
            border-radius: 4px;
            text-transform: uppercase;
            font-size: 14px;
            color: #252725;
            border: none;
            padding: 10px 23px;
            transition:  .2s ease-in;
            cursor:pointer;
            
        }

        button:hover {
            background: #9fda5b;
            box-shadow: 1px 1px 13px 0px rgba(128, 128, 128, 0.35);
        }

        button:focus {
            border: none;
            outline: none;
        }

        .contact-details {
            padding: 20px 0;
            margin-top: 40px;
        }

        .contact-details .label {
            color: #4FC3F7;
            font-size: 18px;
        }

        .contact-details .hint {
            color: #969696;
            font-style: italic;
            font-size: 14px;
        }

        .data {
            font-weight: bold;
        }

        .contact-details>div {
            margin: 10px 0;
        }
        
    </style>
    </head>
    
    <body>
        <div class="thank-container">
            <div class="done">
                <i class="material-icons">done</i>
                <span>Thank you! We have recieved your payment.</span><br>
            </div>
            <div class="done">
                <span>Transaction #@ViewBag.transactionId
            </div>
            <div>
                <!--<a href="http://core.mark8.media/admin/login" target="_top"><button>Login to CORE</button></a>-->
            </div>
            <div class="contact-details">
                <div>
                    <span class="label">You will need to relogin for new permissions to be applied.</span>
                    </br>
                    <span class="label">Click outside this popup to close it and return to application.</span>
                </div>
            </div>
            <!--<div class="contact-details">
                <span class="data">We've send information about you purchase and access data to your email.</span>
            </div>            -->
        </div> 
    </body>
</html>